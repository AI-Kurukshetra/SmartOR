# CHANGELOG

- [2026-03-14 09:09] Initialized `/doc` tracking files to support implementation workflow.
- [2026-03-14 09:45] Added the SmartOR application scaffold, Tailwind 3 configuration, typed mock healthcare domain model, enterprise overview route, per-hospital command centers, and interactive scheduling UI.
- [2026-03-14 10:01] Added Supabase SSR/auth dependencies, middleware, login/register/callback flows, hospital provisioning logic, a Supabase-backed SmartOR data layer, and the first SQL migration for the multi-hospital schema with RLS.
- [2026-03-14 10:21] Added `scripts/seed-smartor-demo.mjs` and a `pnpm seed:demo` command that creates demo auth users, hospital memberships, and realistic SmartOR operational seed data once the schema exists remotely.
- [2026-03-14 10:31] Applied `supabase/migrations/20260314095347_init_smartor_core.sql` to Supabase project `agnjgojoozdyepakkkhy` and seeded the remote demo dataset successfully.
- [2026-03-14 10:52] Added role-aware dashboard UX: typed app-role validation, centralized permission profiles, reusable sidebar navigation filtered by permissions, role-focus panels, and conditional rendering of network/scheduling/coordination/financial surfaces per role.
- [2026-03-14 10:57] Refined dashboard UI theme and layout with a high-contrast command sidebar, textured content canvas, updated panel elevation, and improved action/navigation affordances across enterprise and hospital command surfaces.
- [2026-03-14 11:00] Added HST-style product framing updates: sidebar quick actions, enterprise platform capability cards, integration posture chips, hospital daily workflow lane metrics, and coordination-adjacent integration/revenue checkpoint panels.
- [2026-03-14 11:03] Reworked dashboard chrome again: introduced a more structured sidebar control-rail composition, role-access telemetry block, refreshed grouped navigation framing, and stage-level shell visuals (`dashboard-stage`, `command-sidebar-shell`) for a stronger layout.
- [2026-03-14 11:11] Refined hospital command-center UX with a new mission-control hero treatment, real-time OR signal tiles, an OR pulse lane section, and supporting visual utility classes (`command-horizon`, `or-pulse-strip`) for stronger scanability and urgency communication.
- [2026-03-14 11:13] Upgraded `DashboardSidebar` to a client-side sticky collapsible rail with icon-only compact mode, then changed enterprise/hospital shells from fixed grid columns to flexible rows so the main canvas expands when the sidebar collapses.
- [2026-03-14 11:17] Adjusted sidebar tab links so hospital-route hash navigation (`#operations`, `#scheduling`, `#coordination`) uses native in-page anchors instead of router links, preventing full dashboard remount on tab click.
- [2026-03-14 11:26] Switched hospital sidebar navigation and quick actions to dedicated module routes (`/operations`, `/scheduling`, `/coordination`) and added `app/hospitals/[hospitalSlug]/scheduling/page.tsx` to complete route-per-module navigation.
- [2026-03-14 11:37] Refactored hospital pages to a shared persistent layout (`app/hospitals/[hospitalSlug]/layout.tsx`) so sidebar/module navigation updates only the module content slot; added role-aware tabs (`hospital-module-tabs.tsx`) and active-link highlighting in `DashboardSidebar`.
- [2026-03-14 11:41] Changed login redirection logic to route authenticated users to their default hospital overview (`/hospitals/[slug]`) from both sign-in action and already-authenticated login checks.
- [2026-03-14 11:43] Split sidebar nav sets by context and added a hospital `Overview` entry pointing to `/hospitals/[slug]`, enabling explicit selected-state highlighting on initial hospital landing.
- [2026-03-14 11:56] Added hospital-route loading UX for tab/module navigation by introducing `app/hospitals/[hospitalSlug]/loading.tsx`, disabling hospital-route prefetch in tab/sidebar links, and adding a short simulated delay in `resolveHospitalPageContext` so skeletons are rendered during transitions.
- [2026-03-14 11:59] Ensured hospital tab-transition skeletons render in the persistent shell by wrapping `children` with `<Suspense fallback={<HospitalRouteLoading />}>` inside `app/hospitals/[hospitalSlug]/layout.tsx` and switching `resolveHospitalPageContext` from `cache(...)` to a plain async function.
- [2026-03-14 12:01] Replaced generic hospital tab loading with tab-specific skeleton UIs by adding `components/smartor/hospital-tab-skeleton.tsx` and per-route loading boundaries for overview/operations/scheduling/coordination.
- [2026-03-14 12:05] Refactored `components/smartor/scheduling-crud-panel.tsx` to modal-based scheduling CRUD UX: compact case list on page with dedicated create and edit dialogs (including delete in edit modal).
- [2026-03-14 12:09] Fixed hospital sidebar Admin Controls routing by pointing it to a new dedicated route (`/hospitals/[hospitalSlug]/admin-controls`) and added role-gated admin-controls page/loading entries.
- [2026-03-14 12:10] Removed the sidebar Quick Launch section from `components/smartor/dashboard-sidebar.tsx`, including associated quick-action generation and icon imports.
- [2026-03-14 12:20] Installed `agent-browser` skill from `https://github.com/vercel-labs/agent-browser` using `npx skills add ... --skill agent-browser --yes --global`; skill is now available under `~/.agents/skills/agent-browser` for Codex usage.
- [2026-03-14 12:23] Enhanced hospital Overview route (`app/hospitals/[hospitalSlug]/page.tsx`) with role-specific command briefing: added `buildRoleOverview(...)` mapping and per-role metric cards/highlights for `hospital_admin`, `or_director`, `scheduler`, `surgeon`, and `staff`.
- [2026-03-14 12:25] Updated hospital Overview UI composition to render all metrics in a single top grid (`topMetrics`) before role briefing content, removing the separated second metrics row below.
- [2026-03-14 12:32] Added persisted basic overview metrics with CRUD across stack: created migration `20260314122600_add_overview_metrics.sql` (table/index/RLS/seed rows), introduced `OverviewMetricSchema` + `HospitalDashboardData.overviewMetrics`, loaded `overview_metrics` in `lib/smartor/data.ts`, added server actions in `app/actions/overview-metrics.ts`, built `components/smartor/overview-metrics-crud-table.tsx`, and wired Overview page cards/table to these records.
- [2026-03-14 12:35] Removed the Overview metrics data table section from `app/hospitals/[hospitalSlug]/page.tsx` per UX request; overview metric cards at top continue to use persisted `overviewMetrics` records.
- [2026-03-14 13:28] Implemented hospital integration settings as a core admin feature: added migration `20260314130500_add_hospital_integrations.sql` (table/indexes/RLS/seed), extended typed domain/data loaders (`IntegrationSettingSchema`, `HospitalDashboardData.integrationSettings`), added server actions in `app/actions/integration-settings.ts`, created `components/smartor/integration-settings-panel.tsx` CRUD UI, and integrated it into `app/hospitals/[hospitalSlug]/admin-controls/page.tsx`; validated flows with `agent-browser` and applied migrations to Supabase via `npx -y supabase db push`.
- [2026-03-14 13:35] Completed a full `agent-browser` route/UI regression pass and implemented remaining core blueprint coverage in app UX: added an auto-generated scheduling conflict-resolution engine section in `components/smartor/schedule-board.tsx`, plus mobile app access packaging through `app/manifest.ts`, `app/icon.svg`, and mobile/PWA metadata in `app/layout.tsx`.
- [2026-03-14 13:37] Refined hospital overview UI/UX layout in `app/hospitals/[hospitalSlug]/page.tsx` with blueprint-mapped mission-control framing: added top hero signal tiles, explicit “Core execution lanes” panel, and clickable module routing cards for Operations/Scheduling/Coordination/Admin Controls.
- [2026-03-14 13:40] Improved hospital sidebar shell behavior for layout/UX: changed `app/hospitals/[hospitalSlug]/layout.tsx` to a full-width stage with left-anchored rail, updated `components/smartor/dashboard-sidebar.tsx` to true full-height desktop rail, and added persistent collapse state via `localStorage` (`smartor.sidebarCollapsed`).
- [2026-03-14 13:44] Added `components/smartor/hospital-shell-header.tsx` and integrated it as a sticky top header in `app/hospitals/[hospitalSlug]/layout.tsx` using sidebar-aligned visual language; expanded `app/hospitals/[hospitalSlug]/page.tsx` with a dedicated “MVP scope” coverage checklist; refreshed `app/globals.css` token/gradient palette to a new blue-slate UI theme.
- [2026-03-14 13:46] Simplified header UX in `components/smartor/hospital-shell-header.tsx` by removing duplicated module tabs and retaining only hospital context + status chips + Sign out action; removed duplicate sign-out control from `components/smartor/access-banner.tsx`.
- [2026-03-14 13:47] Removed signed-in banner content for authenticated mode in `components/smartor/access-banner.tsx` and made sticky header placement flush-to-top by adjusting header shell classes in `components/smartor/hospital-shell-header.tsx`.
- [2026-03-14 13:51] Implemented coordination MVP role-based readiness CRUD: added `app/actions/case-readiness.ts` (`updateCaseReadinessAction` with Zod validation + hospital membership + manager-role write gating), created `components/smartor/case-readiness-crud-panel.tsx` modal CRUD UI, and integrated role-gated mutate controls in `app/hospitals/[hospitalSlug]/coordination/page.tsx`.
- [2026-03-14 13:55] Redesigned sidebar information architecture in `components/smartor/dashboard-sidebar.tsx`: split route links into modular grouped cards (`Clinical modules`, `Management`), reduced visual density, and relabeled “Operations board” to “Operations” for faster scanability while preserving role-based gating and collapse behavior.
- [2026-03-14 14:01] Reorganized operations UX into modular routes: simplified `components/smartor/operations-grid.tsx` to a focused command board (summary metrics + live rooms + conflict queue + progression snapshots), added `components/smartor/operations-resources-grid.tsx`, created `app/hospitals/[hospitalSlug]/operations-resources/page.tsx` (+ loading), and added an `Ops resources` sidebar entry in `components/smartor/dashboard-sidebar.tsx`.
- [2026-03-14 14:04] Converted non-metric operations surfaces from cards to tables: in `components/smartor/operations-grid.tsx` switched live room board, conflict queue, and progression snapshots to tabular layouts; in `components/smartor/operations-resources-grid.tsx` switched surgeon availability, instrument readiness, and staffed-room sections to tabular layouts with horizontal overflow support.
- [2026-03-14 14:12] Added table-level CRUD implementation for operations domains: introduced `app/actions/operations-crud.ts` with manager-role-gated server actions for operating rooms, conflicts, surgeons, staff members, and equipment; refactored `components/smartor/operations-grid.tsx` and `components/smartor/operations-resources-grid.tsx` into client-side table CRUD surfaces with Add/Edit/Delete modal workflows; updated route pages (`app/hospitals/[hospitalSlug]/operations/page.tsx`, `app/hospitals/[hospitalSlug]/operations-resources/page.tsx`) to pass hospital slug + role-based mutate flags.
- [2026-03-14 14:25] Refined blueprint/MVP and role-visibility behavior in `app/hospitals/[hospitalSlug]/page.tsx`: replaced static MVP checklist chips with a structured 20-feature status matrix (Implemented/Partial/Pending + coverage notes), added permission keys to overview module cards and filtered them by role, and made Command Routing lines permission-filtered; re-ran `pnpm seed:demo` to refresh all demo role accounts for QA and validated role-based module hide/show + route access controls with `agent-browser` across `hospital_admin`, `or_director`, `scheduler`, `surgeon`, and `staff`.
- [2026-03-14 14:28] Simplified hospital Overview content in `app/hospitals/[hospitalSlug]/page.tsx`: removed the remaining blueprint-tracks panel and all Casetabs blueprint coverage references from the page, restricted top metrics to `basicOverviewMetrics` only, and removed unused role-metric construction/state to keep the Overview focused on core cards + role brief highlights.
- [2026-03-14 14:33] Performed a project-wide UI/UX redesign pass: refreshed color/system tokens and utility primitives in `app/globals.css` + `tailwind.config.ts`, rebuilt shared visual components (`components/smartor/panel.tsx`, `metric-card.tsx`, `section-heading.tsx`, `status-pill.tsx`, `role-focus-panel.tsx`), redesigned navigation chrome (`components/smartor/dashboard-sidebar.tsx`, `hospital-shell-header.tsx`, `app/hospitals/[hospitalSlug]/layout.tsx`, `components/smartor/enterprise-overview.tsx`), improved auth experience styling (`components/auth/auth-shell.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/register/page.tsx`), and upgraded operations/ops-resources table + action button styling (`components/smartor/operations-grid.tsx`, `components/smartor/operations-resources-grid.tsx`); validated with `agent-browser` screenshots on `/`, `/login`, and `/register` (desktop + mobile viewport).
- [2026-03-14 14:39] Updated the visual system again using healthcare-web references captured by `agent-browser` (`tmp-refshots/`): switched app fonts in `app/layout.tsx` to `Manrope` (display) + `Source Sans 3` (body), retuned clinical palette and reduced decorative noise in `app/globals.css`, adjusted shell and heading density/radius in `components/smartor/dashboard-sidebar.tsx`, `components/smartor/hospital-shell-header.tsx`, `components/smartor/panel.tsx`, `components/smartor/section-heading.tsx`, `components/smartor/enterprise-overview.tsx`, and `app/hospitals/[hospitalSlug]/layout.tsx`, and normalized auth emphasis surfaces in `components/auth/auth-shell.tsx`; validated final UI with fresh `agent-browser` screenshots in `tmp-shots-v2/`.
- [2026-03-14 14:41] Refined sidebar-specific UX and color feedback: replaced remaining blue-heavy sidebar gradients in `app/globals.css` (`.command-sidebar`, `.command-sidebar-shell`) with neutral slate tones, improved sidebar information hierarchy and context in `components/smartor/dashboard-sidebar.tsx` (explicit hospital card, reduced descriptive clutter, updated access iconography), and strengthened nav-state readability with cleaner active/hover treatment; validated with new `agent-browser` captures in `tmp-shots-sidebar/`.
- [2026-03-14 14:50] Added a new hospital admin settings route `app/hospitals/[hospitalSlug]/admin-controls/api-keys/page.tsx` (+ loading boundary) with a Supabase-style API key management layout component (`components/smartor/api-keys-settings-layout.tsx`), wired `API keys` navigation in `components/smartor/dashboard-sidebar.tsx`, and retuned global accent styling away from blue to emerald/teal in `app/globals.css` and `tailwind.config.ts`; validated route rendering via `agent-browser` screenshot (`tmp-shots/screenshot-1773480054310.png`).
- [2026-03-14 14:55] Fixed visibility regressions in visual tokens by replacing invalid alpha token syntax in `app/globals.css` (`rgba(var(--token), x)` with valid `rgb(var(--token) / x)`), restoring button/background rendering (including `.btn-primary` on auth forms), updated sidebar shell/background gradients to a deeper green-charcoal treatment, and normalized invalid Tailwind opacity utilities (`bg-accent/12`, `bg-accent/8`) to supported values in `components/smartor/api-keys-settings-layout.tsx`, `components/smartor/metric-card.tsx`, `components/smartor/hospital-module-tabs.tsx`, and `components/smartor/operations-grid.tsx`; verified login button contrast via `agent-browser` screenshot (`tmp-shots/screenshot-1773480306420.png`).
- [2026-03-14 14:57] Corrected `.btn-primary` in `app/globals.css` to use a concrete gradient (`linear-gradient(120deg, rgba(9,22,42,.96), rgba(18,38,66,.95))`) after verifying the tokenized declaration was still not applied in runtime; revalidated via `agent-browser` computed-style check + screenshot (`tmp-shots/screenshot-1773480462157.png`).
- [2026-03-14 15:06] Replaced the root `/` page with a Casetabs-blueprint-aligned marketing landing experience in `app/page.tsx` (hero, operations snapshot stats, must-have features, and strategic capabilities), changed unauthenticated root behavior to render this landing page while still redirecting authenticated users (`mode: "supabase"`) to their hospital route, and validated desktop/mobile output via `agent-browser` screenshots (`tmp-shots/screenshot-1773480953951.png`, `tmp-shots/screenshot-1773480956336.png`); additionally removed an unused sidebar icon import in `components/smartor/dashboard-sidebar.tsx` to keep lint checks passing.
- [2026-03-14 15:07] Added a dedicated top header in `app/page.tsx` for the landing page with branded context plus explicit `Login` and `Register` links, then verified text visibility and rendered output using `agent-browser` (`tmp-shots/screenshot-1773481059752.png`).

- [2026-03-14 15:09] Updated landing header CTA styling in `app/page.tsx` by redesigning the `Register` button as the visual primary action (emerald gradient, directional icon, hover elevation, and accessible focus ring), then revalidated `/` with `agent-browser` (`tmp-shots/screenshot-1773481142457.png`).
- [2026-03-14 15:17] Delivered Operations live-view CRUD refinement in `components/smartor/operations-grid.tsx`: added computed per-room live assignment mapping from case data to render all OR statuses with explicit current and upcoming procedure context, replaced the single mixed case table with dedicated `Current procedures` and `Upcoming cases` tables (both with Edit/Delete actions), and retained in-workspace Add case CRUD modal flow for end-to-end operations management; validated with `agent-browser` plus desktop/mobile screenshots (`tmp-shots/screenshot-1773481614847.png`, `tmp-shots/screenshot-1773481615847.png`).
- [2026-03-14 15:29] Added manager-only hospital sidebar CRUD routing in `components/smartor/dashboard-sidebar.tsx` by introducing `Rooms`, `Surgeons`, `Staff users`, and `Hospital overview` entries gated with new `manager_tabs` permission (`lib/smartor/permissions.ts`), added anchor targets in `components/smartor/operations-grid.tsx` (`#rooms`) and `components/smartor/operations-resources-grid.tsx` (`#surgeons`, `#staff-users`), and created a dedicated manager overview-metrics CRUD route at `app/hospitals/[hospitalSlug]/hospital-overview/page.tsx` (+ loading boundary); validated role visibility and route rendering with `agent-browser`.
